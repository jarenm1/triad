[package]
name = "triad-py"
version = "0.1.0"
edition = "2024"
description = "Python-facing C ABI bindings for Triad simulation configuration"

[lib]
name = "triad_py"
crate-type = ["cdylib", "rlib"]

[dependencies]
triad-gpu = { workspace = true }
triad-sim = { workspace = true }
pollster = "0.3"
